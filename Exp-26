#include <stdio.h>

int main() {
    FILE *fp;
    char data[100];

    fp = fopen("file.txt", "w");
    fprintf(fp, "Hello File Management");
    fclose(fp);

    fp = fopen("file.txt", "r");
    fscanf(fp, "%[^\n]", data);
    fclose(fp);

    printf("File Content: %s\n", data);
    return 0;
}
