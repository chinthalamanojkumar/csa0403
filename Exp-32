#include <stdio.h>

int main() {
    int pages[] = {1, 2, 3, 1, 4};
    int frame[3] = {-1, -1, -1};

    for (int i = 0; i < 5; i++) {
        frame[i % 3] = pages[i];
        printf("Frames: %d %d %d\n", frame[0], frame[1], frame[2]);
    }
    return 0;
}
