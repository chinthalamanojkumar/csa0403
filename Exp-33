#include <stdio.h>

int main() {
    int pages[] = {2, 3, 1, 5};
    int frame[2] = {-1, -1};

    for (int i = 0; i < 4; i++) {
        frame[i % 2] = pages[i];
        printf("Frames: %d %d\n", frame[0], frame[1]);
    }
    return 0;
}
