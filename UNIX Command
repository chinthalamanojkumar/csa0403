#include <stdio.h>

int main() {
    char *files[] = {".", "..", "demo.txt", "main.c", "output.exe"};
    int i;

    for (i = 0; i < 5; i++) {
        printf("%s\n", files[i]);
    }

    return 0;
}
